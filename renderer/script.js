// Executed in the browser context

console.log(process.versions.electron);

document.getElementById('createSecret').addEventListener('click', function() {
    alert('You just clicked the create secret button! :)');
});

